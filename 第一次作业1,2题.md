1.一元线性回归模型： 总体模型：\(y_i=\beta_0+\beta_1 x_i+\varepsilon_i\) 样本回归方程：\(\hat y_i=\hat\beta_0+\hat\beta_1 x_i\) 残差定义：\(\boldsymbol{e_i = y_i-\hat y_i}\)

最小二乘法的目标：最小化残差平方和 \(RSS=\sum_{i=1}^n e_i^2=\sum_{i=1}^n(y_i-\hat\beta_0-\hat\beta_1 x_i)^2\) 对\(\hat\beta_0,\hat\beta_1\)求偏导并令导数 = 0，得到正规方程组：

\(\begin{cases} \displaystyle\frac{\partial RSS}{\partial \hat\beta_0}= -2\sum_{i=1}^n(y_i-\hat\beta_0-\hat\beta_1 x_i)=0\\[4pt] \displaystyle\frac{\partial RSS}{\partial \hat\beta_1}= -2\sum_{i=1}^n x_i(y_i-\hat\beta_0-\hat\beta_1 x_i)=0 \end{cases}\)

代入残差 \(e_i=y_i-\hat\beta_0-\hat\beta_1 x_i\)：

1. 第一式：

\(\sum_{i=1}^n e_i = 0\)

1. 第二式：

\(\sum_{i=1}^n x_i e_i = 0\)



2.

决定系数：

\(R^2=1-\frac{RSS}{TSS}\) \(TSS=\sum(y_i-\bar y)^2\)，总离差平方和，只和 y 有关，增加自变量不会改变 TSS。

调整决定系数：

\(R^2_{adj}=1-\frac{RSS/(n-k-1)}{TSS/(n-1)}\) n是样本量，k是自变量个数；\(n-k-1\)为残差自由度。

1. \(R^2\)为什么不降（上升或不变） OLS 的特性：增加回归变量，残差平方和 RSS 一定不会变大，只会减小或者不变。 即使新增特征是完全随机、和 y 无关，OLS 依然会尽量用这个新变量去拟合样本里随机噪声，找到一个系数，使得 RSS 略微下降；极端情况这个变量完全不起作用，RSS 不变。 TSS固定，\(R^2=1-\frac{RSS}{TSS}\)，RSS 下降 → \(R^2\)上升；RSS 不变则\(R^2\)不变。 👉 \(R^2\)不考虑自由度，只要加变量，它就不会变小，天生偏向复杂模型。
2. \(R^2_{adj}\)为什么会下降 \(R^2_{adj}\)对 RSS 做了自由度修正：

- 添加自变量，k增加，残差自由度 \(df_{res}=n-k-1\) 减小。
- 分母\(TSS/(n-1)\)保持不变。
- 分子：\(\dfrac{RSS}{n-k-1}\)。 新增无关变量带来的 RSS 减少量很小，但自由度会明显变小；\(\frac{RSS}{n-k-1}\) 反而变大。 于是 \(1-\text{（变大的数）}\)，最终\(R^2_{adj}\)下降。